# hpc-bridge

> **Author:** Chittaranjan Baral ([@SJLNS](https://github.com/SJLNS))
> **Version:** v0.3.0
> **Last updated:** 2026-08-30

gRPC bridge to HPC-2 over the Ethernet backbone, mTLS 1.3, VSS subtree filtering.

Build scaffolding: `CMakeLists.txt` + `src/main.cpp` (C++17), verified to compile
and run. gRPC/protobuf logic and mTLS 1.3 credential loading are TODOs in main.cpp.
