# cloud-gateway

> **Author:** Chittaranjan Baral ([@SJLNS](https://github.com/SJLNS))
> **Version:** v0.3.0
> **Last updated:** 2026-08-30

gRPC/HTTPS bridge from HPC-1's local VSS tree to AWS IoT Core, mTLS 1.3.

Build scaffolding: `CMakeLists.txt` + `src/main.cpp` (C++17), verified to compile
and run. AWS IoT Core client logic and mTLS 1.3 credential loading are TODOs in main.cpp.
